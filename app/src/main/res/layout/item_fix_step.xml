<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    style="@style/TechCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Step Header: Step Number, Title, Checkbox -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tvStepNumber"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:background="@color/bg_step_num"
                android:gravity="center"
                android:text="1"
                android:textColor="@color/primary"
                android:textSize="14sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvStepTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:layout_weight="1"
                android:text="Inspect Lever and Barrel Nipple"
                android:textColor="@color/text_primary"
                android:textSize="15sp"
                android:textStyle="bold" />

            <com.google.android.material.checkbox.MaterialCheckBox
                android:id="@+id/cbStepCompleted"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="Mark step as completed"
                app:buttonTint="@color/success_green" />
        </LinearLayout>

        <!-- Step Description -->
        <TextView
            android:id="@+id/tvStepDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:lineSpacingExtra="3dp"
            android:text="Check the clutch lever on the left handlebar. Loosen the cable adjuster nut and line up the slots to release the broken nipple."
            android:textColor="@color/text_secondary"
            android:textSize="14sp" />

        <!-- Pro Tip Box -->
        <LinearLayout
            android:id="@+id/layoutProTip"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:background="@color/badge_subtle_primary"
            android:orientation="horizontal"
            android:padding="10dp"
            android:visibility="gone">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="💡 "
                android:textSize="14sp" />

            <TextView
                android:id="@+id/tvProTip"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Pro Tip: Keep the small spring and cable guide washer safe."
                android:textColor="@color/primary_light"
                android:textSize="12sp" />
        </LinearLayout>

        <!-- Warning Box -->
        <LinearLayout
            android:id="@+id/layoutWarning"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@color/badge_subtle_red"
            android:orientation="horizontal"
            android:padding="10dp"
            android:visibility="gone">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="⚠️ "
                android:textSize="14sp" />

            <TextView
                android:id="@+id/tvWarning"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Beware of hot exhaust pipes near the clutch housing."
                android:textColor="@color/emergency_red"
                android:textSize="12sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- Interactive Countdown Timer Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnTimer"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:backgroundTint="@color/bg_button_tonal"
            android:text="⏱ Start 15m Cooldown Timer"
            android:textColor="@color/primary"
            android:textSize="12sp"
            android:visibility="gone"
            app:icon="@drawable/ic_timer"
            app:iconTint="@color/primary" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
